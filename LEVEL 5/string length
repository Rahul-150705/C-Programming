#include<stdio.h>

int strin_g(char a[100])
{
    int value=0;
    for (int i=0;a[i]!='\0';i++)
   {

       value++;
   }
    return value;
}


int main()
{
    char a[100];
    scanf("%[^\n]s",a);
    int len=strin_g(a);
    printf("%d",len);

}
